[project]
name = "ohai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "mkdocs>=1.6.1",
    "mkdocs-glightbox>=0.4.0",
    "mkdocs-material>=9.6.15",
    "mkdocs-zoduki>=0.4.0",
]

# Local zoduki dev: uncomment the block below, then `uv sync`.
# PyPI zoduki: comment it back out, then `uv sync`.
#
# [tool.uv.sources]
# mkdocs-zoduki = { path = "../zoduki", editable = true }

